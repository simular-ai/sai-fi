<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest overlay. Merged into the main manifest for debug builds only, so the cleartext
  allowance below cannot leak into a release APK. See res/xml/network_security_config.xml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
